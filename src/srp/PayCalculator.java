package srp;

import java.time.LocalTime;

public class PayCalculator extends Employee{
    public PayCalculator(String employeeName, LocalTime start, LocalTime end) {
        super(employeeName, start, end);
    }
}
